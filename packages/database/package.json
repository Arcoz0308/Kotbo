{
  "name": "@kotbo/database",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "migrate:repair-production": "prisma db execute --file prisma/repairs/20260404193000_add_code_police_rules.sql && (prisma migrate resolve --applied 20260404193000_add_code_police_rules || true) && prisma db execute --file prisma/repairs/20260406090000_add_interest_profiles_and_feedback.sql && (prisma migrate resolve --applied 20260406090000_add_interest_profiles_and_feedback || true)",
    "push": "prisma db push",
    "studio": "prisma studio",
    "seed": "bun run prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.6.0",
    "@prisma/client": "^7.0.0",
    "dotenv": "^17.4.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "prisma": "^7.0.0"
  }
}
